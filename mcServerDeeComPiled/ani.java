import com.mojang.datafixers.DSL.TypeReference;

public enum ani {
   a(asr.a),
   b(asr.b),
   c(asr.c),
   d(asr.d),
   e(asr.e),
   f(asr.f),
   g(asr.g),
   h(asr.h),
   i(asr.i),
   j(asr.j),
   k(asr.A),
   l(asr.k);

   private final TypeReference m;

   private ani(TypeReference $$0) {
      this.m = $$0;
   }

   public TypeReference a() {
      return this.m;
   }
}
