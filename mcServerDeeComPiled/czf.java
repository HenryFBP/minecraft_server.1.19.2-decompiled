import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface czf<T> extends AutoCloseable {
   CompletableFuture<cza<T>> a(cge var1);

   void a(cza<T> var1);

   void a(boolean var1);

   default void close() throws IOException {
   }
}
