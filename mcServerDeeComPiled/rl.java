import java.util.UUID;
import javax.annotation.Nullable;

public record rl(UUID b, @Nullable buf c) {
   public static final rl a = new rl(ad.c, null);

   public boolean a() {
      return a.equals(this);
   }
}
